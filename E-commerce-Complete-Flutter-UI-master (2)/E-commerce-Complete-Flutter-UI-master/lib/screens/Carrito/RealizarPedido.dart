import 'dart:convert';

import 'package:flutter/material.dart';
import 'package:http/http.dart' as http;


class RealizarPedido extends StatefulWidget {
  static const String routeName = '/RealizarPedido';

  const RealizarPedido({Key? key}) : super(key: key);

  @override
  State<RealizarPedido> createState() => _RealizarPedido();

  
}

class _RealizarPedido extends State<RealizarPedido> {
  @override
  Widget build(BuildContext context) {
    // Aquí puedes devolver el widget que deseas renderizar
    return Container(
      // Aquí puedes definir la estructura de tu widget
    );
  }
}
